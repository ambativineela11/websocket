WebSocket Live Chat Demo

A simple real-time chat application built using WebSockets to demonstrate persistent, bi-directional communication between clients.

🧠 Why WebSockets?

Unlike polling-based approaches, WebSockets keep a single connection open and allow the server to push updates instantly — making them ideal for real-time features like chat.

🛠 Tech Stack
Node.js
Express
WebSocket (ws)
HTML & Vanilla JavaScript

🚀 How to Run
npm install
node server.js

Open http://localhost:3000 in two browser windows and start chatting.
